# 《镇锁》Phase 3 Copywriting v1
## 第三阶段主线文案正文（影子证据与 OA 真相）

> **文档职责**
> 本文档包含 Phase 3 阶段所有核心深层记录的实际展示文案。
> 研发与 UI 团队可直接复制本文档中的文本进行页面装配。
> **注意**：带有 `[ ]` 的部分为视觉与排版指示。此阶段 UI 必须彻底褪去网页的包装，呈现出纯文本终端、代码块或生硬的工程表盘质感。

---

### 09. README · 留给后来的人 (Record ID: `SA-README-01`)
* **触发方式**：通过搜索前置隐藏词（如 `fswltz` 或 `处理单申诉` 关联）拉出的证据包根目录。
* **UI 容器**：极其简陋的纯文本文件视图，没有排版，只有刺眼的终端绿色或白色等宽字体。

**[页面正文]**

> `===================================`
> `>>> DIRECTORY: /shadow_vol_1/`
> `>>> AUTHOR: [ID_8023]`
> `>>> STATUS: UPLOAD_INTERRUPTED`
> `===================================`
> 
> 如果你能通过那个搜索框强行把这页拉出来，说明我已经不在机房了。
> 
> 不要试图用常用词去搜系统全貌，检索索引早就被他们清洗过了。你在外网看到的“正常”和“已处理”，全都是粉饰过的数据。
> 
> 我没有权限，也没有时间做全库快照。我只能在系统每次触发[覆写指令]的瞬间，把“被改掉的那行字”强行截走，拼成**证据**。
> 
> 别看他们给你看的东西。看差异。
> 
> 去搜 `gh_0314_lx 状态变更`。看看昨天下午 14:02 到底发生了什么。

---

### 10. 差异节选记录 · gh_0314_lx (Record ID: `SA-ARCH-GH-01`)
* **触发方式**：在全局搜索框输入组合词 `gh_0314_lx 状态变更`。
* **UI 容器**：标准的“证据包”容器。带有抓取乱码、明确的 DIFF（差异）高亮，以及底部的赵启残注。

**[页面正文]**

> `===================================`
> `>>> CAPTURE_DUMP: SYS_TICK_1709402`
> `>>> TARGET: PATIENT_REL_gh_0314_lx`
> `>>> WARN: Data block partially corrupted during forced extraction.`
> `===================================`
> 
> `[14:02:11] API_CALL: update_patient_status (Target_ID: LX-044-YIN)`
> 
> `[系统覆写前 / OLD_VALUE]:`
> `{`
> `  "status": "极度排斥，已注射镇静剂",`
> `  "location": "A区常规病房",`
> `  "dnr_sync": "false"`
> `}`
> 
> `[当前公开值 / NEW_VALUE]:`
> `{`
> `  "status": "家属自愿办理出院",`
> `  "location": "null",`
> `  "dnr_sync": "true"`
> `}`
> 
> `...[后续心率监控数据段获取失败，已被物理切断]...`
> 
> `>>> MANUAL_NOTE_APPENDED (8023_残注):`
> 他们在论坛里对家属撒谎，在系统里给自己撒谎。14:02 根本没有出院记录，人是在那时候被推下去的。
> 想知道人去了哪，别查病房，去查 **B2 节点负载**。那里的机器需要“燃料”。

---

### 11. B2 区域报告 (Record ID: `OA-B2-01`)
* **触发方式**：在全局搜索框输入组合词 `B2 节点负载`。
* **UI 容器**：冰冷的数据监控表盘。没有任何人性化的描述，只有生硬的服务器负载指标和状态指示灯。

**[页面正文]**

> **[B2 物理阵列 · 实时负载监控]**
> `> UPDATE: 实时同步中`
> 
> | 物理节点 | 算力占用 | 神经同步池 | 当前作业目标 | 状态 |
> | :--- | :--- | :--- | :--- | :--- |
> | NODE_01 (贪狼) | 98.2% | 饱和 | 意识降维 | 运行中 |
> | NODE_02 (巨门) | 97.5% | 饱和 | 意识降维 | 运行中 |
> | NODE_03 (禄存) | 99.1% | 临界 | 意识降维 | 运行中 |
> | ...`[受限投影：中间节点索引被屏蔽]`... |
> | NODE_07 (破军) | 94.0% | 注入中 | 接收实体 [LX-044-YIN] | **负载激增** |
> 
> **[系统级警告]**
> 当前矩阵算力已逼近结构阈值。请确认 **DNR 内部释义** 协议是否已全量覆盖新接入的耗材实体。
> 主控程序 **TaiYi** 正在请求更高优先级的资源调度，以压制节点共振带来的物理损伤。

---

### 12. 术语说明 · DNR 内部释义 (Record ID: `OA-DNR-01`)
* **触发方式**：在全局搜索框输入组合词 `DNR 内部释义`。
* **UI 容器**：极度反人性的工程技术白皮书片段。将医学词汇彻底剥离出人类伦理范畴。

**[页面正文]**

> **[安宁深眠内部工程词典 - 释义投影]**
> 
> **词条：DNR (Deep Neural Relocation / 深度神经重定向)**
> *注：对外文案统一包装为“Deep Non-REM (深度无梦睡眠)”，切勿混淆。*
> 
> **核心定义**：
> 并非医疗手段。这是一种算力/意识的强行剥离与**转移**协议。
> 通过特定的物理频段干预，将高敏感度的人类焦虑意识作为“高频算力”，强行剥离出物理躯体，并定向引导至 B2 的七个接收**节点**中。
> 
> **执行与损耗**：
> 实体（患者）在 DNR 协议下将陷入不可逆的脑死亡状态（对外口径：长效深眠）。其剥离出的神经算力将被用于供养主系统。
> 
> **终局控制预案**：
> 若实体意识发生不可控的集体反扑，系统保留一项涉及底层根架构的物理熔断指令。该指令代号已从本文档中移除，仅存于 **TaiYi** 主控核心的离线校验模块中。
> `[警告：相关执行对象的完整结构需七节点校验闭合后方可调用，任何片段式尝试将被系统静默丢弃。]`